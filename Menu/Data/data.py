# Zoraa Dev/Dev Knowing

try:
    import requests
    import json
    import re
    import time
    import os
    import sys
except (ImportError, ValueError) as e:
    exit(f'[•] Module Error: {str(e).title()}')